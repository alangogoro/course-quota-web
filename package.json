{
  "name": "course-quota-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cindy's table-tennis course tracker — a local-only Android mobile web tool.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.10"
  }
}
